# v0.1.0 — initial release

- Keepsake registry (11 entries across all five keep kinds, lapse-named).
- Staged fail-closed intake with set-aside notes.
- Staged evaluator: `read_succession`, `read_succession_with_findings`.
- Deterministic serialization and digests; invariant battery.
- Four deterministic SVG figures; formalism claim ledger (derived).
- Witness envelopes and line_set binding declaration (prepared inputs).
- Full test suite, no mocks, >= 90% coverage of src.
