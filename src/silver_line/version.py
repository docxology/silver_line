"""Version markers for the Silver Line package.

The package version tracks the code surface; the registry digest (see
``serialization.registry_digest``) tracks the keepsake content. Reviewers can
pin both when citing a succession reading.
"""

__version__ = "0.1.0"
